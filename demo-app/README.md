This is a Flask app to issue a trust claim

Installation
---------------

create python virtual environment if not already set up

```
python3 -m venv venv
. venv/bin/activate
pip3 install -r requirements.txt
```

Running
----------------

`flask run`
